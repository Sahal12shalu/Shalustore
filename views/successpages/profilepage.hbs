<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }
        .profileall{
            width: 100%;
            margin-top: 50px;
            justify-items: center;
            margin-left:10px;+
        }

        .profile-container {
            width: 90%;
            max-width: 400px;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .profile-img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #3498db;
        }

        .profile-info {
            margin-top: 20px;
        }

        .profile-info h2 {
            font-size: 24px;
            color: #333;
        }

        .profile-info p {
            font-size: 16px;
            color: #777;
            margin: 5px 0;
        }
        .profileheader{
            display: ;
        }
        .changeprofile{
            border-radius: 20px;
            height: 40px;
            width: 140px;
            background-color: #3498db;
            border: none;
            color: white;
        }
    </style>
</head>
<body>
    <div class="profileall">
    <div class="profile-container">
        <form action="/changeprofile" method="post" enctype="multipart/form-data">
        <label class="profileheader" for="profileImage">
            <img src="{{#if (eq details.[0].status 'placed')}}images/profileimage/{{details.[0]._id}}.jpg{{else}}images/unknown.png{{/if}}" alt="Profile Image" class="profile-img" id="profilePic">
        </label>
              <div class="mb-3  col-12">
                <label class="form-label">Upload Image</label>
                <input type="file" name="image" class="form-control" id="productImage" required>
                </div>
        <div class="mt-3">
        <button class="changeprofile">change profile</button>
        </div>
        </form>
        {{#each details}}
        <div class="mt-5 profile-info">
            <h2 style="font-family: 'Times New Roman', Times, serif;"><strong>{{this.name}}</strong></h2>
            <p class="mt-3">{{this.email}}</p>
            <h6 class="mt-3">{{this.mobile-number}}</h6>
        </div>
        {{/each}}
    </div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

</body>
</html>
